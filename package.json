{
  "name": "design-token-example",
  "version": "1.0.0",
  "author": "",
  "watch": {
    "build": ["**/*.json"]
  },
  "scripts": {
    "build": "npm run build:token-transform; npm run build:token-style-dictionary",
    "build:token-transform": "token-transformer tokens.json tokens/input.json",
    "build:style-dictionary": "style-dictionary build --config config.json",
    "watch": "npm-watch"
  },
  "dependencies": {
    "npm-watch": "^0.9.0",
    "style-dictionary": "^3.0.2",
    "token-transformer": "^0.0.27"
  }
}
